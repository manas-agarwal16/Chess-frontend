import React from 'react';
import {Login as LoginComp} from '../components';

const Login = () => {
    return (
        <div>
            <LoginComp />
        </div>
    );
}

export default Login;
