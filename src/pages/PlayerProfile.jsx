import React from "react";
import { PlayerProfile as Profile } from "../components";

const PlayerProfile = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default PlayerProfile;
